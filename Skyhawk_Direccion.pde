// Las 4 direcciones en las que se puede mover el avion.
// Usar un enum (en vez de numeros sueltos) hace el codigo mas claro:
// se lee "Direccion.ARRIBA" en vez de, por ejemplo, 0.
enum Direccion
{
  ARRIBA, ABAJO, IZQUIERDA, DERECHA
}
